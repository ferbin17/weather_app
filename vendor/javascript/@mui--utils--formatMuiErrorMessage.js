// @mui/utils/formatMuiErrorMessage@7.2.0 downloaded from https://ga.jspm.io/npm:@mui/utils@7.2.0/esm/formatMuiErrorMessage/index.js

/**
 * WARNING: Don't import this directly. It's imported by the code generated by
 * `@mui/interal-babel-plugin-minify-errors`. Make sure to always use string literals in `Error`
 * constructors to ensure the plugin works as expected. Supported patterns include:
 *   throw new Error('My message');
 *   throw new Error(`My message: ${foo}`);
 *   throw new Error(`My message: ${foo}` + 'another string');
 *   ...
 * @param {number} code
 */
function r(r, ...e) {
  const o = new URL(`https://mui.com/production-error/?code=${r}`);
  e.forEach((r) => o.searchParams.append("args[]", r));
  return `Minified MUI error #${r}; visit ${o} for the full message.`;
}
export { r as default };
